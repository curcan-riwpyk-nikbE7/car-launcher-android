# Car Launcher — лаунчер для автомагнитолы на Android 8.0+ (CC3-style)

Кастомный домашний экран (launcher) для 2DIN автомагнитол на Android,
дизайн воссоздан по референсу лаунчера в стиле **Teyes CC3**: плеер в виде
смартфона с неоновой RGB-рамкой по контуру, карточка авто со спидометром
(MPH) на фоне неоновой дороги, белые плитки быстрых приложений, радио-виджет
с волновым визуализатором, боковая рейка с часами/ассистентом/навигацией.

## Возможности

- **Регистрируется как HOME/Launcher** — можно выбрать домашним экраном системы.
- **Плеер оформлен как "экран смартфона"** — тёмный прямоугольник с
  многоцветной неоновой рамкой по периметру (cyan → purple → pink),
  Bluetooth-бейдж, название/исполнитель реального трека, полоска
  прогресса, крупные кнопки управления снизу.
- **Работающие жесты на рабочем столе** (окончательно исправлен баг):
  - свайп влево/вправо — переключение на следующий/предыдущий трек;
  - свайп вверх/вниз — увеличение/уменьшение громкости.

  Техническая причина, почему жесты не работали раньше, и как исправлено:
  `OnTouchListener`, повешенный на `ViewGroup`, вызывается только если ни
  один дочерний View (кнопка, плитка приложения) не "забрал" ACTION_DOWN
  первым — поэтому свайп, случайно начавшийся над кликабельным элементом,
  никогда не долетал до детектора жестов. Решение — обрабатывать жесты в
  `Activity.dispatchTouchEvent()`, который видит вообще все touch-события
  ещё до раздачи по дереву View, и обязательно возвращать `true` из
  `GestureDetector.onDown()` (иначе Android прерывает разбор жеста на
  первом же касании и `onFling()` не вызывается).
- **Реальные данные плеера и статус-бара** — название трека, исполнитель и
  источник (Bluetooth/Яндекс.Музыка) берутся из `MediaSessionManager`;
  дата, статус Bluetooth/Wi-Fi и громкость — из системных API.
- **Плитки приложений больше не превращаются в белые квадраты с сырым
  именем пакета**, если ярлык по умолчанию (Яндекс.Навигатор, системное
  FM-радио и т.п.) не установлен на конкретном устройстве. Раньше
  `PackageManager` не мог отдать ни иконку, ни название для отсутствующего
  пакета — теперь `ShortcutManager` хранит заранее заданную "визитку"
  (красивая иконка + человекочитаемое название) для популярных приложений
  и использует её как fallback, а для реально установленных — берёт
  настоящую иконку/имя через `PackageManager`.
- **Анимированная карточка машины с дорогой** — пятая версия. Четвёртая
  версия (готовое изображение машины + лёгкое покачивание) была
  подтверждена пользователем реальным скриншотом как визуально
  правильная, но не давала ощущения движения — по просьбе пользователя
  добавлены дорога и вращение колёс:
  - **процедурная псевдо-3D дорога** с перспективным сведением к точке
    схода и **бегущей дорожной разметкой**: на скорости 0 км/ч разметка
    почти неподвижна (небольшая "холостая" анимация, чтобы карточка не
    выглядела мёртвой), с ростом реальной скорости с GPS
    (`SpeedProvider`/`LocationManager`) разметка "летит" на зрителя
    быстрее — так же, как ощущение скорости за окном автомобиля;
  - **вращающиеся колёсные диски**: сначала было опробовано вращать сам
    вырезанный фрагмент фотографии колеса — при повороте это давало
    заметные швы с кузовом (проверено и отклонено на этапе прототипа,
    см. `car_geometry_check/`), поэтому вместо этого поверх фото-шины на
    исходной картинке рисуется чистая **векторная геометрия диска**
    (эллипс-обод в цветах интерфейса + спицы + ступица), координаты и
    радиусы под каждое колесо измерены программно по цвету неоновой
    обводки диска на изображении (`car_geometry_check/measure_wheels.py`),
    а не подобраны на глаз — поэтому диск садится в шину без сдвига и
    вращается без швов; скорость вращения зависит от реальной скорости
    с GPS;
  - вся композиция (дорога + машина + положение и радиус каждого колеса)
    была заранее собрана и лично просмотрена через Pillow на нескольких
    кадрах анимации (наклон кузова, разные углы поворота дисков) —
    в отличие от OpenGL, здесь API рисования (эллипсы/полигоны/альфа-
    композитинг) идентичны между Pillow и `android.graphics.Canvas`,
    поэтому расхождения между тем, что проверено, и тем, что реально
    рисуется на устройстве, не возникает;
  - также сохранены: покачивание на "подвеске", пульсирующее неоновое
    свечение под колёсами, наклон корпуса пропорционально скорости
    (имитация инерции при разгоне/торможении);
  - **честное техническое ограничение**: это по-прежнему не полноценная
    3D-сцена со свободным вращением камеры и не фотореалистичная модель
    конкретного авто — но теперь карточка передаёт ощущение движения
    (дорога + вращающиеся колёса), а не просто статичную картинку.

- **Synthwave/outrun-стиль фона карточки (шестая версия)** — пользователь
  прислал скриншот референсного лаунчера с ярким сиянием на линии
  горизонта, расходящимися неоновыми лучами и перспективной сеткой, и
  попросил сделать "как здесь". Плоская дорога с разметкой (пятая
  версия) заменена на такой фон:
  - вертикальный + радиальный градиент создаёт яркое фиолетовое сияние
    вокруг точки схода на линии горизонта, с чёткой светлой линией-ядром;
  - веер из ~13 расходящихся неоновых лучей (сине-фиолетовых оттенков)
    с мягким свечением через `BlurMaskFilter`;
  - перспективная сетка: сходящиеся в точку схода линии статичны,
    горизонтальные линии **едут на зрителя** (перспективное сжатие),
    скорость по-прежнему зависит от реальной скорости с GPS;
  - статичная часть фона (градиент, лучи, сходящиеся линии) кэшируется
    один раз в отдельный `Bitmap`/`Canvas` при изменении размера
    карточки — это нужно, т.к. `BlurMaskFilter` для мягкого свечения
    лучей не работает на аппаратно-ускоренном `Canvas` самого `View`,
    но полностью работает на обычном software-канвасе поверх Bitmap;
    анимируются (перерисовываются каждый кадр) только едущие
    горизонтальные линии сетки — это сохраняет плавность анимации без
    пересборки всего фона на каждый кадр;
  - машина и вращающиеся колёсные диски сохранены как есть (уже
    подтверждены пользователем ранее), только немного уменьшены и
    подняты, чтобы не перекрывать лучи сверху и не обрезаться по краям;
  - вся композиция была заново собрана и лично просмотрена через
    Pillow, сравнивая бок о бок с присланным пользователем референсом,
    перед переносом на `Canvas` Android
    (`car_geometry_check/synthwave_style_check.png`).

- **Вид на машину сзади с мягким сиянием (седьмая, текущая версия)** —
  пользователь прислал ДРУГОЙ референс (лаунчер Teyes с машиной вида
  строго сзади по центру карточки, мягкий яркий бело-фиолетовый нимб
  света позади силуэта, светящаяся горизонтальная полоса стоп-сигнала,
  тёмная едва текстурированная поверхность под колёсами — без лучей,
  без сетки, без бегущей разметки) и явно указал "должно быть вот так".
  Synthwave-фон с лучами и сеткой (шестая версия) полностью заменён:
  - сгенерировано новое изображение машины строго сзади
    (`car_rear.png`) со встроенным свечением-нимбом и текстурой пола —
    подобрано максимально близко к референсу, обрезано под пропорции
    карточки;
  - карточка перестроена: раньше слева была отдельная непрозрачная
    панель со скоростью/иконками, а картинка машины — отдельным блоком
    справа; теперь картинка машины растянута на всю карточку целиком
    (`center-crop`, без искажения пропорций), а панель со
    скоростью/иконками наложена НАД ней полупрозрачным слоем слева —
    как в референсе;
  - т.к. вид строго сзади не показывает колёса сбоку, вращающиеся
    диски (из пятой/шестой версии) убраны как неприменимые к этому
    ракурсу — вместо них "ощущение движения" передаётся через
    **пульсирующее свечение нимба** позади машины (интенсивность
    пульсации растёт с реальной скоростью с GPS) и лёгкое покачивание
    корпуса на "подвеске"; наклон корпуса при разгоне сделан почти
    незаметным (в отличие от бокового вида) — при взгляде строго сзади
    сильный наклон выглядел бы неестественно;
  - эффект пульсации яркости заранее проверен в Python (наложение
    яркостной маски на несколько "кадров") перед переносом на
    `ColorMatrixColorFilter` в Android — оба линейно масштабируют RGB,
    поэтому поведение идентично;
  - готовое изображение машины после сборки APK было извлечено обратно
    из архива `.apk` и открыто отдельно, чтобы подтвердить, что именно
    оно туда попало (`car_geometry_check/car_rear_final_extracted_from_apk.png`).

- **Дополнительный реальный функционал**:
  - кольцо-кнопка "ассистент" на рейке запускает голосовое распознавание;
  - кнопка навигации внизу рейки открывает Яндекс.Навигатор/Карты или
    Google Карты;
  - иконка-шестигранник на карточке авто ведёт в настройки, иконка фары
    включает/выключает фонарик камеры;
  - карточка "Radio" кликабельна и открывает системное FM-радио.
- **Добавление своих приложений на рабочий стол** — кнопка "+" открывает
  список всех установленных приложений; выбранное закрепляется как ярлык
  (`SharedPreferences`, переживает перезапуск). Долгое нажатие — удалить.
- **Экран "Все приложения"** (`AppDrawerActivity`) — все установленные
  приложения через `PackageManager` с живым поиском.
- **Экран "Настройки лаунчера"** (`SettingsActivity`) — доступ к
  уведомлениям/геолокации (с реальным статусом), тема, единицы скорости
  (MPH/км/ч), сброс рабочего стола.

## Из чего состоит

```
app/src/main/java/com/carlauncher/app/
├── MainActivity.java             — главный экран, статус-бар, жесты, ярлыки
├── ShortcutManager.java          — хранение/добавление/удаление ярлыков рабочего стола
├── ShortcutPickerActivity.java   — экран выбора приложения для "+"
├── SystemStatusHelper.java       — реальные Wi-Fi/Bluetooth/громкость/дата
├── AppDrawerActivity.java        — экран "Все приложения" с поиском
├── SettingsActivity.java         — настройки лаунчера
├── AppItem.java / AppLauncherUtil.java — запуск приложений по пакету
└── media/
    ├── MediaRepository.java         — единая точка правды "что сейчас играет"
    ├── NotificationListener.java    — слушает все MediaSession в системе
    └── NotificationAccessUtil.java  — проверка/запрос разрешения
```

## Важные технические ограничения (актуально для реальных магнитол)

1. **Скорость "0 MPH"** — статичный дизайн-элемент. Для реальной скорости
   нужен GPS (`LocationManager`) или вендорский SDK магнитолы для чтения
   CAN-шины.
2. **Радио-виджет** — UI-заглушка с визуализатором. Реальное управление
   тюнером требует вендорского API конкретной магнитолы.
3. **Bluetooth-захват музыки** работает "из коробки" на любом Android 8+
   через официальный публичный API (`NotificationListenerService` +
   `MediaSessionManager`).

## Сборка APK

```bash
export JAVA_HOME=/path/to/jdk-21
export ANDROID_HOME=/path/to/android-sdk
./gradlew assembleDebug     # debug-сборка
./gradlew assembleRelease   # release-сборка, подписана тестовым keystore
```

Готовые собранные копии лежат прямо в репозитории:
- [`dist/CarLauncher-debug.apk`](dist/CarLauncher-debug.apk)
- [`dist/CarLauncher-release.apk`](dist/CarLauncher-release.apk)

### Автосборка через GitHub Actions

При каждом push в `main` запускается [`.github/workflows/build.yml`](.github/workflows/build.yml),
который собирает debug и release APK и прикладывает их как build-артефакты.

## Установка на магнитолу

1. Скопируйте `CarLauncher-release.apk` на магнитолу (флешка / ADB).
2. Установите APK.
3. Нажмите кнопку "Домой" на магнитоле → выберите Car Launcher → "Всегда".
4. При первом запуске разрешите доступ к уведомлениям (для чтения Bluetooth/Яндекс.Музыки).
5. Нажмите "+" в блоке "Application", чтобы добавить свои ярлыки на рабочий стол.

## Лицензия

Демонстрационный/учебный проект. Дизайн вдохновлён публичным интерфейсом
Teyes CC3, иконки и изображения сгенерированы для демонстрации. Свободно
используйте и дорабатывайте под свою магнитолу.
